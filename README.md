# Raphaela & Matheus — Site do Casamento

Site one-page no mesmo visual do convite (sage, oliva e ouro). O HTML do Casar.com ficou em `referencia/` só como rascunho de seções.

## Abrir localmente

```bash
cd "/home/karla/Área de trabalho/Dev/site-raphaela-matheus"
python3 -m http.server 8081
```

Acesse http://localhost:8081

O convite continua em `../Rapha` (porta 8080).
